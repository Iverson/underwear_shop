<h1>Listing Brands</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Country</th>
  </tr>

<% @brands.each do |brand| %>
  <tr>
    <td><%= brand.name %></td>
    <td><%= brand.country.name %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Project', new_brand_path %>