<h1>Listing Sections</h1>

<table>
  <tr>
    <th>Name</th>
  </tr>

<% @sections.each do |section| %>
  <tr>
    <td><%= section.name %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Section', new_section_path %>