<h1>Listing countries</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
  </tr>

<% @countrys.each do |country| %>
  <tr>
    <td><%= country.name %></td>
    <td><%= link_to 'Destroy', country, method: :delete, data: { confirm: 'Вы уверены?' } %></td>
  </tr>
<% end %>
</table>

<br />
